export interface Organization {
  id: string;
  name: string;
  city: string;
  state: string;
  country: string;
  phoneNumber: string;
  salvations: number;


  // users[]
  // tasks[]
  // owner
}
